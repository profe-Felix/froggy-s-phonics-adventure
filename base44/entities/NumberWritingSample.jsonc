{
  "name": "NumberWritingSample",
  "type": "object",
  "properties": {
    "student_number": {
      "type": "integer"
    },
    "class_name": {
      "type": "string"
    },
    "school_year": {
      "type": "string",
      "description": "School year tag e.g. '26-27'. Prevents cross-year collisions when the same class+number is reused.",
      "default": ""
    },
    "number": {
      "type": "integer"
    },
    "image_url": {
      "type": "string",
      "description": "Canvas snapshot URL"
    },
    "stroke_count": {
      "type": "integer"
    },
    "strokes_data": {
      "type": "string",
      "description": "JSON-encoded array of strokes, each stroke is an array of {x,y,t} points"
    }
  },
  "required": [
    "student_number",
    "class_name",
    "number"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "student"
          }
        }
      ]
    },
    "read": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "teacher"
          }
        }
      ]
    },
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}